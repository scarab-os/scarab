# Description: Network file retriever
# URL: https://www.gnu.org/software/wget/
# Depends:

name=wget
version=1.25.0
source=https://ftp.gnu.org/gnu/wget/wget-$version.tar.gz

build() {
    cd $name-$version
    ./configure --host=$CROSS --prefix=/usr --without-ssl --without-libpsl \
        --disable-nls --disable-pcre --disable-iri \
        CFLAGS="-Os" LDFLAGS="-static"
    make -j$(nproc)
    make DESTDIR=$PKG install-strip
}
